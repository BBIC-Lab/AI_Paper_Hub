---
title: "MaskedFOP: Polyglot Speaker Identification under Missing Visual Modality via Cascaded Graph Label Propagation"
title_zh: MaskedFOP：通过级联图标签传播在缺失视觉模态下进行多语言说话人识别
authors: "Ayoub Elkhouzari, Youssef Iraqi, Loubna Mekouar"
date: 2026-06-12
pdf: "https://arxiv.org/pdf/2606.14321v1"
tags: ["query:ndai", "paper:通用启发"]
score: 7.0
evidence: 说话人识别中缺失模态的领域适应
tldr: 针对测试时人脸缺失且语音语言未见的闭集多语言说话人识别挑战，提出的MaskedFOP系统通过模态丢弃训练、多模型嵌入集成和两阶段图标签传播推理，在POLY-SIM 2026挑战中以0.9989的P-accuracy取得第一，证明了其在缺失模态和跨语言场景下的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.14321v1/fig-001.webp\", \"caption\": \"Figure 2: MaskedFOP inference: two-stage cascaded label propagation. Two frozen seeds (s1, s2) encode each test clip, audio embeddings are ℓ2-averaged into ã (Eq. 4) and the fused embedding z̃ is taken from s1. Stage 1 (P3, P5) refines labels by graph LP over the test set (𝐾=7, 𝛼=0.65, 50 iters), seeded from training-prototype neighbors. Cascade seeding replaces the 70 training prototypes with the ∼1,500 high-confidence Stage-1 labels (>20× denser, in-domain), the single largest gain (+8pp). Stage 2 (P4, P6) assigns labels by cosine nearest-centroid to these centroids, no further LP.\", \"page\": 5, \"index\": 1, \"width\": 1002, \"height\": 373}]"
motivation: 解决测试时视觉模态完全缺失且语音来自训练未见过语言时的闭集多语言说话人识别问题。
method: 采用模态丢弃双头网络训练鲁棒音频编码，结合多模型嵌入平均与两阶段级联图标签传播推理，用域内测试集质心优化标签分配。
result: 系统在POLY-SIM 2026挑战上获得平均P-accuracy 0.9989，排名第一。
conclusion: 提出的MaskedFOP有效应对缺失模态和跨语言挑战，级联播种策略是性能最大增益来源，代码已开源。
---

## 摘要
我们提出了MaskedFOP，这是一个用于闭集多语言说话人识别的系统，面临两个并行的挑战：测试时完全缺失人脸模态，且语音来自乌尔都语——一种在人脸监督训练中未出现的语言。该系统集成了三种互补机制。首先，基于融合与正交投影（FOP）骨干构建的模态丢弃双头网络，通过逐样本人脸掩码强迫音频分支发展独立的判别能力，确保在人脸缺失时音频编码器仍能正常工作。其次，两个使用不同随机种子的基于强调通道注意力、传播与聚合的时延神经网络（ECAPA-TDNN）特征训练的MaskedFOP实例，产生互补的音频嵌入，其逐元素平均产生比任何单一模型更鲁棒的512维表示。第三，两阶段级联推理过程首先通过融合图标签传播（GLP）传递（第一阶段）细化多模态标签，然后通过余弦最近质心（第二阶段）分配仅音频标签，用第一阶段中约1500个域内测试集质心替换70个稀疏训练原型。该系统提交至POLY-SIM 2026大挑战，在挑战服务器上评估的所有提交中，取得了0.9989的平均P-accuracy，排名第一。消融实验确定级联播种是最大的增益来源（在P4/P6上超过8个百分点）。代码可在https://github.com/Ayoub-Elkhouzari/POLY-SIM2026获取。

## Abstract
We present MaskedFOP, a system for closed-set polyglot speaker identification under two simultaneous challenges: the face modality is entirely absent at test time, and speech comes from Urdu, a language unseen during face-supervised training. The system integrates three complementary mechanisms. First, a modality-dropout dual-head network built on the Fusion and Orthogonal Projection (FOP) backbone forces the audio branch to develop independent discriminative power via per-sample face masking, ensuring that the audio encoder remains capable when face is absent. Second, two MaskedFOP instances trained on Emphasized Channel Attention, Propagation, and Aggregation in Time Delay Neural Network (ECAPA-TDNN) features with different random seeds produce complementary audio embeddings whose element-wise average yields a more robust 512-dimensional representation than any single model. Third, a two-stage cascaded inference procedure first refines multimodal labels through a fused Graph Label Propagation (GLP) pass (Stage 1), then assigns audio-only labels by cosine nearest-centroid (Stage 2), replacing the 70 sparse training prototypes with ~1,500 in-domain test-set centroids from Stage 1. Submitted to the POLY-SIM 2026 Grand Challenge, the system achieves a mean P-accuracy of 0.9989, placing first among all submissions evaluated on the challenge server. An ablation identifies cascaded seeding as the single largest gain (>8 pp on P4/P6). The code is available at https://github.com/Ayoub-Elkhouzari/POLY-SIM2026.