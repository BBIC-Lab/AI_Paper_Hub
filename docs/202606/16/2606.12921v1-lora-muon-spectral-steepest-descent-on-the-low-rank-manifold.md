---
title: "LoRA-Muon: Spectral Steepest Descent on the Low-Rank Manifold"
title_zh: LoRA-Muon：低秩流形上的谱最速下降法
authors: "Franz Louis Cesista, Katherine Crowson, Cédric Simal, Stella Biderman"
date: 2026-06-11
pdf: "https://arxiv.org/pdf/2606.12921v1"
tags: ["query:ndai", "paper:通用启发"]
score: 7.0
evidence: LoRA-Muon用于可迁移低秩微调
tldr: 低秩适配（LoRA）微调虽降低资源消耗，但采用因子化优化器时存在初始化敏感、学习率跨秩迁移差等问题。本文提出LoRA-Muon，将Muon优化器的谱最陡下降规则应用于低秩场景，并配合分裂权重衰减。该方法作为全秩Muon/Shampoo的高效代理，实现了学习率跨秩、宽度、深度等泛化，且在TinyShakespeare实验中以秩32配置超越全秩基线，同时避免QR分解和二阶矩存储，更适配加速器且内存高效。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.12921v1/fig-001.webp\", \"caption\": \"Figure 1: Our LoRA-Muon update implicitly applies the Muon update of the product W = ABT and projects it onto the tangent space of the low rank manifold. As a result, our update is invariant under gauge transformations of the LoRA factors.\", \"page\": 2, \"index\": 1, \"width\": 582, \"height\": 231}, {\"url\": \"assets/figures/arxiv/2606.12921v1/fig-003.webp\", \"caption\": \"Figure 2: Alignment of the two tangent components ∆ABT and A∆BT across training in the TinyShakespeare LoRA-Muon study. The components rapidly become and remain strongly aligned, so the triangle-inequality relaxation behind the η/2 split stays nearly tight throughout training rather than loosening over time.\", \"page\": 4, \"index\": 3, \"width\": 664, \"height\": 336}, {\"url\": \"assets/figures/arxiv/2606.12921v1/fig-002.webp\", \"caption\": \"Figure 3: Validation loss versus learning rate across the four learning-rate-transfer sweeps: rank, width, depth, and scalar gauge scale. LoRA-Muon matches dense Muon’s best tested learning rate across all four sweeps, making it a cheap proxy for hyperparameter tuning before running larger-scale training runs.\", \"page\": 8, \"index\": 2, \"width\": 826, \"height\": 659}, {\"url\": \"assets/figures/arxiv/2606.12921v1/fig-005.webp\", \"caption\": \"Figure 4: Paired validation-loss comparisons with scalar gauge rebalancing disabled versus enabled for LoRA-Muon and Spectron after initializing the random LoRA factors at gauge scale 9 by multiplying one factor and dividing the other. LoRA-Muon’s loss distribution is essentially unchanged by rebalancing, while Spectron’s changes substantially, consistent with Section 5.1.\", \"page\": 9, \"index\": 5, \"width\": 822, \"height\": 273}, {\"url\": \"assets/figures/arxiv/2606.12921v1/fig-004.webp\", \"caption\": \"Table 1: Coefficient sequence for Algorithm 3, which realizes the Muon-style matrix-sign map msign(·) by a polynomial Newton–Schulz iteration after Frobenius normalization. Each row gives the coefficients for one orthogonalization step in the numerical realization used in our experiments.\", \"page\": 17, \"index\": 4, \"width\": 826, \"height\": 558}]"
motivation: LoRA使用因子化优化器（如AdamW）时面临初始化敏感、学习率难以跨秩迁移、常弱于全秩训练等挑战。
method: 将Muon优化器的谱最陡下降规则引入低秩自适应，并结合分裂权重衰减策略。
result: 在TinyShakespeare上，秩2代理即可复现全秩最佳学习率，秩32 LoRA-Muon的平均验证损失低于全秩基线。
conclusion: LoRA-Muon是有效的低秩代理优化器，学习率可迁移且计算存储更优，优于现有方案。
---

## 摘要
低秩适应（LoRA）显著降低了深度学习模型微调的计算和内存成本，但通常比密集训练更难调优：当使用因子级优化器（如 AdamW）时，它对初始化选择敏感，其最优学习率在不同秩间迁移性差，且往往无法超越密集基线。我们通过将 Muon 优化器的谱最速下降规则应用于低秩设置，推导出 LoRA-Muon。结合我们的分割权重衰减规则，我们主要主张 LoRA-Muon 是全秩 Muon 和 Shampoo 系列优化器的良好低秩替代。其最优学习率可在秩、宽度、深度和因子重缩放之间迁移。在我们的计算匹配 TinyShakespeare 研究中，秩为 $2$ 的替代恢复了密集测试的最佳学习率，而秩为 $32$ 的 LoRA-Muon 运行在种子平均扫描中获得了比密集基线更低的平均验证损失。我们进一步表明，Spectron 优化器依赖于任意的因子缩放，因此当微调从严重不平衡的因子开始时，它可能不太适合；并且 LoRA-RITE 的简化 QR 坐标核心实现了相同的谱更新。LoRA-Muon 无需 QR 分解即可计算该更新，并避免存储二阶矩，使其更适应加速器且内存效率更高。

## Abstract
Low-Rank Adaptation (LoRA) significantly reduces compute and memory costs for finetuning Deep Learning models but is often harder to tune than dense training: when using factor-wise optimizers such as AdamW, it is sensitive to initialization choices, its optimal learning rates transfer poorly across ranks, and it often fails to beat dense baselines. We derive LoRA-Muon by applying the Muon optimizer's spectral steepest-descent rule to the low-rank setting. Along with our split weight-decay rule, our main claim is that LoRA-Muon is a good low-rank proxy for full-rank Muon and Shampoo-family optimizers. Its optimal learning rates transfer across rank, width, depth, and factor-rescaling. In our compute-matched TinyShakespeare study, a rank-$2$ proxy recovers the dense best tested learning rate, and a rank-$32$ LoRA-Muon run attains lower mean validation loss than the dense baseline in the seed-averaged sweep. We further show that the Spectron optimizer depends on arbitrary factor scaling, so it would likely be a poor fit when finetuning starts from badly imbalanced factors, and that LoRA-RITE's simplified QR-coordinate core implements the same spectral update. LoRA-Muon computes that update without QR-decomposition and avoids storing second moments, making it more accelerator-friendly and memory-efficient.

---

## 论文详细总结（自动生成）

## 研究价值与阅读建议
- **关联方向**：弱相关；本文聚焦低秩微调的优化器设计，属通用深度学习训练方法，不直接针对脑机接口、神经解码等特定领域。
- **启发与意义**：若在线神经信号建模存在资源受限的低秩适配需求，本文揭示的谱最速下降方向和跨秩可迁移学习率可能改善参数搜索成本。
- **可借鉴点**：低秩流形上的约束优化思想、独立于因子坐标的更新机制，或可为神经表征漂移的快速校准提供新视角。
- **阅读建议**：重点阅读第 3–4 节的方法推导和对称性分析；对于非优化器研究者，其几何分解和代理实验范式亦有参考价值。

## 1. 论文的核心问题与整体含义
- **研究背景**：低秩适应（LoRA）通过仅微调低秩因子大幅降低模型微调的算力和存储成本，但常用的因子级优化器（如 AdamW）面临调参困难：初始化敏感、最优学习率难以在不同秩之间迁移、且经常无法超越全秩训练基线。
- **核心问题**：如何设计一个在低秩约束下的优化器，使得其最优学习率能在不同秩、宽度、深度和因子缩放之间良好迁移，同时保持甚至超越全秩训练的收敛质量。
- **整体含义**：将 Muon 优化器的谱最速下降规则从全矩阵空间推广到低秩流形上，提出 LoRA-Muon，作为全秩 Muon 和 Shampoo 系列优化器的高效低秩代理，解决 LoRA 调参痛点并提升性能。

## 2. 论文提出的方法论
- **核心思想**：直接在低秩矩阵流形 $M_r$ 的切空间上定义约束下的最速下降子问题，而非在因子坐标空间（$A$、$B$）独立优化。利用酉不变范数的性质，将约束下的线性最小化神谕（LMO）作用于投影后的梯度，得到与全秩 Muon 几何一致的更新规则。
- **关键技术细节**：
  - **切空间分解**：权重更新 $\Delta W = \Delta A B^\top + A \Delta B^\top$，将总信任域半径 $\eta$ 均分给两个方向，从而独立求解两个子问题。
  - **投影形式更新**：泛化到任意酉不变范数，得 $\Delta A^\star B^\top = \frac{\eta}{2} \text{LMO}_{\|\cdot\|}(G_t P_B)$ 且 $A \Delta B^\star{}^\top = \frac{\eta}{2} \text{LMO}_{\|\cdot\|}(P_A G_t)$，其中 $P_A$、$P_B$ 为因子列空间投影矩阵。
  - **谱特例 LoRA-Muon**：取谱范数，并借助梯度关系 $ \nabla_A f = G_t B$ 等，得因子更新：
    $$\Delta A^\star = -\frac{\eta}{2} \text{msign}\bigl(\nabla_A f\, S_B^{-1/2}\bigr) S_B^{-1/2}, \quad S_B = B^\top B,$$
    $$\Delta B^\star = -\frac{\eta}{2} \text{msign}\bigl(\nabla_B f\, S_A^{-1/2}\bigr) S_A^{-1/2}, \quad S_A = A^\top A.$$
  - **分割权重衰减**：为避免因子级衰减导致乘积权重过度收缩，采用 $A_{t+1}=sA_t+s^{-1}\Delta A^\star$（$s=\sqrt{1-\lambda\eta}$），使作用于 $W=AB^\top$ 的权重衰减与全秩情形一致。
  - **规范不变量**：上述更新对任意可逆的规范变换 $(A,B)\mapsto(AR, B R^{-\top})$ 不变，保证了优化对因子表示的鲁棒性。
- **算法流程**：算法 1 给出了实用步骤：计算动量、求 $S_A$、$S_B$ 的 Newton–Schulz 逆平方根、谱符号正交化，再结合分割权重衰减更新因子。

## 3. 实验设计
- **数据集与场景**：TinyShakespeare 字符级语言建模，使用因果 Transformer（带 RoPE、QK 归一化、RMS 归一化），侧重低秩微调对超参数搜索的加速。
- **基准与对比方法**：
  - 密度基线：全秩 Muon 训练。
  - 低秩方法：LoRA-Muon（本文）、Spectron（谱重归一化）及 LoRA-RITE 的简化 QR 核（等价更新但实现不同）。
- **评测指标**：种子平均验证损失、最佳学习率的一致性。
- **计算匹配**：所有 LoRA 运行的步数按全秩运行的浮点运算量进行匹配，保证比较公平。

## 4. 资源与算力
- 文中未明确说明使用的 GPU 型号、数量和具体训练时长。仅提及实验为 “TinyShakespeare-scale”，且以 $10^6$ 令牌总预算进行种子平均扫描。因此算力规模较小，属于验证性实验。

## 5. 实验数量与充分性
- 实验组包括：不同秩（1,2,4,8,16,32）、不同模型宽度、不同层数、不同初始因子缩放尺度的学习率扫描，以及规范重平衡和敏感度检验。
- 每点采用 $6$ 个随机种子，报告标准误差，学习率在对数尺度上精细扫描。
- 实验剖析了等式（12）的形式及其计算对比（Spectron、LoRA-RITE），并提供了理论证明和消融性对比（例如重平衡与否的配对比较），整体设计充分、客观且具有内部对比性。但覆盖数据集和模型规模有限，偏向小规模验证。

## 6. 论文的主要结论与发现
- 即使在秩 $2$ 下，LoRA-Muon 也能恢复全秩 Muon 的最佳学习率，实现了超参数的完全可迁移性。
- 在所有扫描的秩、宽度、深度和因子缩放中，LoRA-Muon 的最佳学习率保持一致。
- 在秩 $32$ 时，计算匹配的 LoRA-Muon 平均验证损失显著低于全秩基线（$1.776$ 对 $1.789$）。
- 证明了 Spectron 非规范不变，对因子尺度敏感；而 LoRA-RITE 的简化 QR 核代数上与 LoRA-Muon 等效，但后者更内存高效、计算更简单。

## 7. 优点
- **几何驱动的设计**：直接从低秩流形的切空间出发，使优化独立于因子表示，并获得规范不变性，解决了 LoRA 中初始化敏感和超参数迁移难的问题。
- **高效且稳定**：仅需一阶矩，无需 QR 分解或昂贵二阶矩存储，适配加速器；分割权重衰减修正了因子化训练的常见偏差。
- **实证扎实**：在小规模实验中展现了极佳的学习率可迁移性和超越全秩基线的能力，验证了理论。

## 8. 不足与局限
- **实验规模有限**：仅在 TinyShakespeare 数据集上进行，尚缺乏大规模预训练或下游微调任务的验证，其迁移性在大模型上的鲁棒性未知。
- **超参数迁移范围较窄**：主要验证了线性层学习率迁移，其他超参数（如批次大小、动量、显式正则化）的耦合未全面探索。
- **与二阶方法的对比不足**：虽理论提及 Shampoo 家族，但未直接与 Adafactor、Shampoo 等二阶优化器进行实证对比。

## 9. 研究价值与阅读建议（已在第一节说明，此处从略）

（完）
