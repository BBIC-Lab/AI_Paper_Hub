---
title: When Does Routing Become Interpretable? Causal Probes on Block Attention Residuals
title_zh: 路由何时变得可解释？对块注意力残差的因果探测
authors: Aydin Javadov
date: 2026-06-11
pdf: "https://arxiv.org/pdf/2606.13168v1"
tags: ["query:ndai", "paper:通用启发"]
score: 7.0
evidence: 块注意力残差用于可解释性
tldr: 本研究探讨了区块注意力残差（Block AttnRes）中的可解释路由何时变得可解释。通过对比内容无关的固定调度路由与端到端训练的路由，发现只有训练获得的路由才展现出结构化模式，但描述性路由权重与因果重要性存在分离，说明暴露路由张量是必需的，但不足以保证机制解释，需通过因果干预验证。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.13168v1/fig-001.webp\", \"caption\": \"Figure 1. Source-family routing ablations: baseline (recency-biaswrapped vanilla Qwen3) vs. Block AttnRes trained from scratch, on the same probe and the same data. Three families produce strong AttnRes-side effects with negligible baseline counterparts; a fourth carries routing mass but no causal weight.\", \"page\": 2, \"index\": 1, \"width\": 458, \"height\": 280}, {\"url\": \"assets/figures/arxiv/2606.13168v1/fig-003.webp\", \"caption\": \"Figure 2. Sublayer-restricted routing ablations on the AttnRes checkpoint. embedding matters only through MLP; nonlocal only through attention; current through both, with attention larger; prev completed through neither.\", \"page\": 4, \"index\": 3, \"width\": 457, \"height\": 280}, {\"url\": \"assets/figures/arxiv/2606.13168v1/fig-004.webp\", \"caption\": \"Figure 3. Layer-binned sublayer ablations on the AttnRes checkpoint as a heatmap. A small number of localised cells carry the primary causal signal.\", \"page\": 5, \"index\": 4, \"width\": 457, \"height\": 264}, {\"url\": \"assets/figures/arxiv/2606.13168v1/fig-002.webp\", \"caption\": \"Figure 4. Routing-mass composition by sublayer. The baseline allocates ≈ 0.84 to current in both sublayers; the AttnRes attention sublayer is broadly distributed with the largest slice on nonlocal, while the AttnRes MLP sublayer is current-heavy with non-trivial embedding and prev completed slices.\", \"page\": 8, \"index\": 2, \"width\": 943, \"height\": 428}]"
motivation: 路由信息的直接暴露是否足以实现机制解释尚不明确。
method: 对两个规模相同（0.6B）的Block AttnRes模型（一个遵循固定调度，另一个端到端训练）施加相同的路由消融干预，分析路由模式与因果作用。
result: 训练得到的路由形成了三种局部化模式，但平均路由质量与因果重要性严重分离，一处源族虽有较大权重却无因果贡献。
conclusion: 路由的结构化暴露是机制解释的必要不充分条件，描述性路由摘要需经因果干预检验，不能直接视为机制证据。
---

## 摘要
块注意力残差通过将固定的加性残差替换为对较早深度层表征的学习 softmax，使得跨层路由在前向传递中成为一个可检查的张量。这成为一个诱人的可解释性目标：以往只能间接推断的信息流现在可以直接观察。我们探究这种暴露是否足以进行机制解释。我们在相同的路由消融干预下探测了两个相同规模（6亿参数）的块注意力残差检查点：一个是在原版 Qwen3 上通过确定性近因偏置调度进行推理包装——代码库将其作为路由等价的加载路径——另一个是从头开始训练且将路由作为优化一部分的块注意力残差 Qwen3。包装基线的路由权重与内容无关，并复现了调度的解析预测。而经过训练的 AttnRes 检查点则展现出三种局域化的路由模式：一条经由早期层 MLP 的嵌入源通路，一条经由早期层注意力和 MLP 的当前状态通路，以及一条经由后期层注意力的旧历史通路。除这种分层之外，我们发现平均路由质量与因果重要性之间存在明显的分离：在两个子层中，质量最大的切片并非因果贡献最大的切片，并且一个源家族携带可观的质量，但在干预下没有可检测到的因果作用。因此，路由的架构暴露是必要的，但不足以进行机制解释：结构化深度路由仅当路由成为训练的一部分时才会出现，而且即便出现，描述性的路由总结也应被视为需要通过因果干预检验的候选假设，而非其自身的机制证据。

## Abstract
Block Attention Residuals (Block AttnRes) by replace fixed additive residuals with a learned softmax over earlier depth-source representations, surfacing cross-layer routing as an inspectable tensor in the forward pass. This is a tempting interpretability target: information flow normally inferred indirectly is now directly observable. We ask whether such exposure suffices for mechanistic interpretation. We probe two same-scale ($0.6$B) Block AttnRes checkpoints under identical routing-ablation interventions: a vanilla Qwen3 inference-wrapped through a deterministic recency-bias schedule that the codebase admits as a routing-equivalent loading path, and a Block AttnRes Qwen3 trained from scratch with routing as part of optimisation. The wrapped baseline's routing weights are content-independent and reproduce the schedule's analytic prediction. The trained AttnRes checkpoint instead exhibits three localised routing motifs: an embedding-source pathway through early-layer MLP, a current-state pathway through early-layer attention and MLP, and an older-history pathway through late-layer attention. Beyond this stratification, we find a sharp dissociation between average routing mass and causal importance: in both sublayers, the largest mass slice is not the largest causal contribution, and one source family carries appreciable mass with no detectable causal role under intervention. Architectural exposure of routing is therefore necessary but not sufficient for mechanistic interpretation: structured depth routing emerges only when routing has been part of training, and even then, descriptive routing summaries should be treated as candidate hypotheses to be tested by causal interventions, not as evidence of mechanism in their own right.