---
title: "Tyan-WP: A Wind Power Foundation Model for Ultra-Short-Term Probabilistic Forecasting"
title_zh: Tyan-WP：用于超短期概率预测的风电基础模型
authors: "Jiahui Huang, Ao Luo, Lei Liu, Hongwei Zhao, Tengyuan Liu, Ruibo Guo, Bo Wang, Zhao Wang, Bin Li"
date: 2026-06-07
pdf: "https://arxiv.org/pdf/2606.08630v1"
tags: ["query:ndai"]
score: 8.0
evidence: 风电基础模型用于超短期概率预测
tldr: "针对风电预测中站点特定模型泛化差、无法利用站点属性的挑战，本文提出首个风电基础模型Tyan-WP，通过大规模预训练与静态站点嵌入、功率感知气象融合模块，实现零样本超短期概率预测。实验在百余站点显著优于8个监督模型和11个通用大模型，误差降低16%~22%，并在英国站点展现强泛化能力，为新风电场快速并网提供新途径。"
source: arxiv
selection_source: fresh_fetch
motivation: 现有风电预测模型在数据稀缺场景泛化差，且无法有效利用静态站点属性及功率与气象变量的依赖关系。
method: 提出Tyan-WP，基于超12万美国站点七年数据预训练，设计静态站点嵌入和功率感知气象融合模块以提升零样本预测。
result: "Tyan-WP在域内站点上超越8个监督TSM和11个通用LTSM，MAE降低19.9%，并在英国站点验证了跨地理泛化能力。"
conclusion: 风电基础模型可实现无需目标站点训练的准确零样本预测，为新风电场快速部署和概率风险管理提供实用方案。
---

## 摘要
全球风电装机容量，尤其是中国，正在蓬勃发展，新风电场遍布不同地形和气候。业界迫切需要准确的风电基础模型，以缩短调试时间并加速并网。这是因为特定场地的时序模型（TSM）不适合数据稀缺场景且泛化能力差，而通用大时序模型（LTSM）大多局限于单变量输入，无法充分利用静态场地属性或功率与气象协变量间的依赖关系，导致精度不足。为填补这一空白，我们提出了Tyan-WP，首个用于超短期概率预测的风电基础模型。该模型在美国超过126,000个站点、长达七年的风电大数据集上预训练，通过两个领域特定的模块设计进一步提升了零样本预测性能：利用坐标、地形和生态区元数据的静态场地嵌入模块，以及建模历史功率与气象协变量间相互作用的功率感知气象融合（PAMF）模块。在统一的评估协议下，Tyan-WP在10个域内站点上超越了8个特定场地监督TSM，在127个域内站点上优于11个通用LTSM，将MAE降低19.9%，RMSE降低16.6%，CRPS降低22.2%，AQL降低21.7%，同时R²提升16.7%。在六个英国实际站点上，它进一步展示了强大的跨地理泛化能力。这些结果表明，风电基础模型无需目标场地训练即可实现精确的零样本预测，为新风电场的快速风机接入和概率风险管理提供了一条实用途径。

## Abstract
Global wind power capacity, especially in China, is booming, with new farms spanning diverse terrains and climates. The industry urgently needs accurate wind power foundation models to shorten commissioning and accelerate grid connection. This is because site-specific time series models (TSMs) are not well suited to data-scarce scenarios and generalize poorly, while generic large time series models (LTSMs) are mostly limited to univariate inputs and cannot fully exploit static site attributes or the dependencies between power and meteorological covariates, leading to insufficient accuracy. To fill this gap, we propose \textbf{Tyan-WP}, the first wind power foundation model for ultra-short-term probabilistic forecasting. Pretrained on a large-scale wind power dataset covering more than 126,000 U.S. sites over seven years, Tyan-WP further improves zero-shot forecasting through two domain-specific module designs: static site embedding using coordinate, terrain, and ecoregion metadata, and a power-aware meteorological fusion (PAMF) module that models interactions between historical power and meteorological covariates. Under a unified evaluation protocol, Tyan-WP surpasses eight site-specific supervised TSMs on 10 in-domain sites and outperforms eleven generic LTSMs on 127 in-domain sites, reducing MAE by 19.9%, RMSE by 16.6%, CRPS by 22.2%, and AQL by 21.7%, while raising R^2 by 16.7%. It further demonstrates strong cross-geography generalization on six real U.K. sites. These results show that the wind power foundation model can achieve accurate zero-shot forecasting without target-site training, providing a practical pathway for rapid turbine onboarding and probabilistic risk management at new wind farms.

---

## 论文详细总结（自动生成）

## 研究价值与阅读建议
- **关联方向**：弱相关。本文聚焦风电功率的零样本概率预测，而读者方向为脑机接口神经解码，两者在应用层相距较远，但在时序建模、跨域迁移与多变量融合方面存在方法层面的交叉。
- **启发与意义**：展示了如何利用站点静态元数据和动态物理变量融合实现“无需校准”的高质量预测，为BCI中利用个体皮层解剖信息或行为标签辅助跨被试/跨会话神经解码提供了“预训练+静态嵌入”的思路。
- **可借鉴点**：功率感知融合（PAMF）的交叉注意力门控机制可类比为神经动态与行为状态间的交互建模；稀疏专家网络（MoE）可处理不同脑状态或任务下的异构表征；分位数预测直接输出分布，对神经解码的不确定性量化有参考价值。
- **阅读建议**：重点关注领域特定静态嵌入设计、多分支时序编码器以及无损分位数预测头，思考如何将这些模块化领域自适应方法迁移到神经信号的数据稀缺与表征漂移场景，而不必深究风电物理约束部分。

## 1. 论文的核心问题与整体含义
- **背景与动机**：全球风电装机激增，新风场遍布复杂地形与气候，但新投产风机历史数据极为有限，传统的场地特定时序模型（TSM）泛化能力差，而通用大时序模型（LTSM）通常只接受单变量输入，无法利用站点静态属性（坐标、地形）及功率与气象协变量间的物理依赖，导致零样本预测精度不足。
- **核心目标**：提出首个风电领域基础模型Tyan‑WP，仅需预训练一次，即可对任意新站点（无目标站点训练数据）进行超短期（4小时）的概率预测，从而缩短调试周期、加速并网、支撑风险调度。

## 2. 论文提出的方法论
- **整体框架**：模型接收四类输入——历史功率序列、历史气象序列（风速、风向正弦/余弦、空气密度、温度、地表气压）、时间戳以及站点静态元数据（经纬度、地形类型、生态区）。不利用未来气象。输出未来16个时间步（4小时，15分钟分辨率）在9个分位数水平（0.1~0.9）上的预测值。
- **动态‑静态联合嵌入**：
  - **双分支分块嵌入**：功率与气象序列各自独立分词（patch embedding），保留物理含义，防止早期拼接。
  - **时间级日历嵌入**：用分块结束时间戳提取分钟、小时、日期、月份的周期性正弦/余弦特征，通过小型MLP映射为时间嵌入。
  - **场地级地理‑生态嵌入**：将经纬度映射为正弦/余弦特征，地形和生态区分别作可学习查表嵌入，经线性投影和融合得到一个站点全局向量。
- **功率感知气象融合模块（PAMF）**：
  - 先用旋转位置编码（RoPE）自注意力强化功率分支的短时动态 → $\tilde{\mathbf{P}}$。
  - 后用交叉注意力以气象分支为查询、功率分支为键和值，提取与当前功率状态对齐的气象成分 $\mathbf{M}_{\text{cond}}$，得到互补气象信息 $\mathbf{M}_{\text{comp}} = \mathbf{M}^{(0)} - \mathbf{M}_{\text{cond}}$。
  - 最后通过Sigmoid门控融合 $\tilde{\mathbf{P}}$ 和 $\mathbf{M}_{\text{comp}}$，得到融合表征序列。
- **稀疏专家编码器**：融合表征加上时间嵌入，并前置站点嵌入作为“站点令牌”，输入多层稀疏MoE编码器。每层先经RoPE自注意力，再通过Top‑K路由器选择2个专家FFN处理各令牌，辅以负载均衡损失。
- **分位数预测头**：扁平化所有输出令牌，通过一个MLP一次性产生全部 $H \times |Q|$ 的分位值，取 $q=0.5$ 作为点预测。训练损失为多水平分位数损失（pinball loss）加上专家路由的负载均衡KL散度项。

## 3. 实验设计
- **数据集**：
  - **预训练与域内评估**：NREL WIND Toolkit (WTK)，美国大陆126,692站点，2007–2013年，5分钟分辨率经重采样至15分钟。划出127个站点作为域内零样本测试集（其中10个站点用于与监督TSM对比），其余用于预训练。
  - **域外评估**：英国Kelmarsh风场6台Senvion MM92 2.05 MW风机，2016–2024年SCADA数据，重采样至15分钟，并合成空气密度和气压变量。
- **对比方法**：
  - **场地特定监督模型**：FEDformer, DLinear, NSTransformer, TimesNet, PatchTST, iTransformer, TimeXer, TimeMixer，均在10个WTK站点独立训练。
  - **通用大时序模型（零样本）**：MOMENT, TimeMoE, Sundial, TiRex, TimesFM 2.5, Chronos 2, Moirai 2.0, Timer‑S1，均使用单变量功率输入（仅Moirai 2.0支持历史协变量），禁用未来气象。
- **评估协议**：统一输入窗口16小时（64步），预测4小时（16步），指标含MAE、RMSE、R²、CRPS、AQL（均在原始MW尺度），所有模型无未来气象输入，分位点 $q=0.5$ 为点预测。

## 4. 资源与算力
- **训练硬件**：2× AMD EPYC 9B14 96核CPU，1.5 TiB内存，8× NVIDIA RTX PRO 6000 Blackwell GPU（每卡96 GB）。
- **训练配置**：35.53 M参数，MoE激活参数约13.17 M，batch size 26,624，训练20个epoch，优化器AdamW，学习率5e-5，梯度裁剪1.0。全文未明确给出总训练时间或功耗数据。

## 5. 实验数量与充分性
- 共进行了**三组主要实验**：① WTK 10站点零样本 vs. 8个场地特定监督TSM（每个站点5随机种子）；② WTK 127站点零样本 vs. 11个通用LTSM；③ Kelmarsh 6站点域外零样本 vs. 通用LTSM。
- 额外设计了**四组消融实验**：替换PAMF为通道内注意力、替换为通道独立模块、移除站点嵌入、将稀疏MoE替换为密集FFN，分别在两大测试集上验证。
- 对比基线涵盖不同类型、不同规模的模型，评估指标兼顾点预测和概率预测，并在统一协议下进行，保证了公平性和全面性，实验量充足。

## 6. 论文的主要结论与发现
- **零样本全面最优**：Tyan‑WP在域内10站点上超越所有场地特定监督TSM（MAE降低13.3%等）；在127站点零样本对比中超越所有通用LTSM（MAE降低19.9%，CRPS降低22.2%）；在英国域外站点上依然保持最优或接近最优的综合性能，展现了跨地理、跨数据源的泛化能力。
- **模块有效性**：消融证实PAMF在两类数据上均贡献显著，站点嵌入在站点多样性高时收益更明显，稀疏专家编码器对域外泛化至关重要（替换为密集FFN后误差增加超过20%）。
- **领域预训练的价值**：直接借用通用时序大模型无法满足风电需求，而大规模风电数据上的领域预训练配合领域特定归纳偏置，是实现精准零样本预测的关键。

## 7. 优点
- **首创性**：首个面向超短期概率预测的风电基础模型，填补了领域空白。
- **方法新颖性**：PAMF模块通过功率‑气象互补性建模避免了信息冗余，静态站点嵌入利用了地理生态先验，稀疏MoE增强了多风况适应性。
- **评估严谨**：对比了代表性最强的监督模型和通用大模型，采用统一协议，涵盖域内和域外迁移，并用消融验证了每个组件的独立贡献。
- **实用价值突出**：零样本能力可直接加速新风电场的并网和风险量化，模型与权重均已开源。

## 8. 不足与局限
- **训练数据源差异**：预训练依赖美国数值模拟数据（WTK），与实际SCADA记录存在分布差异，域外迁移（英国）的提升幅度相对有限，仍有改进空间。
- **气象变量依赖**：模型假设能获取站点级历史气象观测，而现实中新建站点可能仅有风场级气象数据；也未利用数值天气预报（未来气象），限制了更长时间尺度的扩展。
- **站点元数据映射**：跨域应用时地形和生态区类别需手动代理映射，可能损失部分先验信息。
- **概率标定未深入讨论**：虽然采用分位数回归，但未系统检验预测分布的可靠性或进行严格的统计检验。
- **计算资源需求较高**：使用8块高端GPU训练，小型团队复现可能存在门槛，且训练时长未明确，难以评估碳足迹。

（完）
